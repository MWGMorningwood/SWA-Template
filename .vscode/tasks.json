{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "func: host start",
            "type": "shell",
            "command": "func start",
            "isBackground": true,
            "problemMatcher": "$func-watch",
            "options": {
                "cwd": "${workspaceFolder}/api/SendEmailFunction"
            }
        },
        {
            "label": "swa: start",
            "type": "shell",
            "command": "swa start src --api-location api",
            "isBackground": true,
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}